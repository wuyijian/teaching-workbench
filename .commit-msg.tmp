fix(upload): 修复文件选择器双触发 + 清理死代码

修复:
- TaskPanel.CreateForm 上传 dropzone: 把 input 改 hidden 不再做透明蒙层,
  之前外层 div onClick 与 input 原生 click 同时触发, Web 模式会让
  系统文件选择器打开两次。改为 ref.click() 编程触发 (单次入口)。
- 加无障碍: role=button + tabIndex=0 + Enter/Space 键触发。

清理:
- 删除死代码 src/components/FileUploadPanel.tsx 与 RecordingPanel.tsx
  (无任何 import 引用, 旧版独立录音页遗留)。

bump 1.0.1 -> 1.0.2
